# Copy this file to .env and fill in real values. Never commit .env.

DATABASE_URL=postgresql://revenueos:revenueos@localhost:5432/revenueos

# Razorpay TEST MODE keys only (https://dashboard.razorpay.com/app/keys)
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=xxxxxxxxxxxxxxxxxxxx
RAZORPAY_WEBHOOK_SECRET=xxxxxxxxxxxxxxxxxxxx

# Optional: only needed if you wire the agents to a real LLM instead of the
# rule-based reasoning shipped by default (see app/agents.py)
ANTHROPIC_API_KEY=
